const express = require("express");
const router = express.Router();

router.post("/api/mail", (req, res) => {
  //TODO: Add in Nodemailer to handle receipt of the email and to send it on to me.
});

module.exports = router;
